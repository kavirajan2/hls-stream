label: HLS (Single Bitrate)

inputs:
  - name: dir
    label: Create Directory
    type: string
    required: true
    
    
tasks:       

  - type: bash
    label: Create new directory
    script: | 
      mkdir ${dir}
